# Data

Run `python download_data.py` from the repository root to place the official
UCI workbook here as `Folds5x2_pp.xlsx`. The notebook also performs this step
automatically when the workbook is absent.
